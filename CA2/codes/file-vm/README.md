# VM3: File Server

This virtual machine acts as a simple HTTP file server. It serves static assets like images and text files to the Web VM over the network.

## Setup & Run
1. Open a terminal in this directory.
2. Initialize the Go module (run once):
   ```bash
   go mod init filevm